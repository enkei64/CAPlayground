{
  "name": "caplayground-desktop",
  "version": "1.0.0",
  "dependencies": {
    "electrobun": "latest"
  },
  "private": true,
  "scripts": {
    "dev": "bun x electrobun build && electrobun dev",
    "start": "electrobun dev",
    "dist": "electrobun build --env=stable",
    "build:canary": "electrobun build --env=canary"
  },
  "trustedDependencies": [
    "register-scheme"
  ]
}